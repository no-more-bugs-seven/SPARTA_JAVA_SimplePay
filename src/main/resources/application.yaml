spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}  # .env 또는 환경변수로 override 가능, 기본값 local
  application:
    name: PaymentApp

  # 클라이언트 API 계약 설정 파일 import
  config:
    import: optional:classpath:client-api-config.yml

  security:
    user:
      name: demo
      password: demo123

server:
  port: 8080

# PortOne Configuration
portone:
  api:
    base-url: https://api.portone.io
    secret: ${PORTONE_API_SECRET:77uSt2bet8A85E3C3vr1rt9AT1EC4Yg5D1Pm1LvdnxmxYDWF1qZE0zEipped0BpdQIvPll0pA8nz86pi}
  store:
    id: ${PORTONE_STORE_ID:store-2fa52df5-a7b3-4c8e-b9c8-06a71d8d876f}
  channel:
    kg-inicis: ${PORTONE_CHANNEL_KG:channel-key-df4170fd-637c-4509-b111-6ac8b41b4bcf}
    toss: ${PORTONE_CHANNEL_TOSS:channel-key-d3e2a0bd-206e-459e-af37-d3b4c54ada9c}
# JWT Configuration
jwt:
  secret-key: ${JWT_SECRET_KEY:L6h/xMxGO53HQmR/OB99qR/y0kTr2CAyaI5RqBIX184=}
  access-token-validity-time: 3600000    # 1시간 (ms)
  refresh-token-validity-time: 604800000 # 7일 (ms)

# UI Branding Configuration
app:
  ui:
    branding:
      app-name: CommerceHub
      tagline: 통합 커머스 플랫폼
      logo-text: 🏪 CommerceHub

logging:
  level:
    com.bootcamp.paymentdemo: DEBUG
    org.springframework.security: INFO
